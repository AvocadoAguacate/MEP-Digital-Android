<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mep_digital">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MEPDigital">
        <activity
            android:name=".Student.Student_teacherActivity"
            android:exported="true" />
        <activity
            android:name=".Student.List_classActivity"
            android:exported="true" />
        <activity
            android:name=".Student.Student_classActivity"
            android:exported="true" />
        <activity
            android:name=".Admin.Detail.ClassDetailActivity"
            android:exported="true" />
        <activity android:name=".Admin.Detail.StudentDetailActivity" />
        <activity android:name=".Admin.Detail.TeacherDetailActivity" />
        <activity
            android:name=".Admin.AdminActivity"
            android:label="@string/title_activity_admin" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>